name: Refresh content (good news)
on:
  schedule:
    - cron: '0 6 * * 1,4'   # Mondays + Thursdays 06:00 UTC
  workflow_dispatch: {}    # manual run button in the Actions tab
permissions:
  contents: write
jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Selftest (offline logic checks)
        run: node scripts/refresh-content.mjs --selftest
      - name: Refresh news from outlet RSS feeds
        run: node scripts/refresh-content.mjs
      - name: Rebuild the website from content.json
        run: node scripts/build-site.mjs
      - name: Commit and push if changed
        run: |
          if git diff --quiet; then
            echo "No changes this week."
          else
            git config user.name "hopeling-bot"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add hopeling-web/content.json today wins atlas species guardians facts site.css sitemap.xml site-data
            git commit -m "Auto-refresh good news + rebuild website (bot run)"
            git push
          fi
